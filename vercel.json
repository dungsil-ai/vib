{
  "buildCommand": "if [ \"$VERCEL_ENV\" = \"production\" ]; then prisma db push && prisma generate && next build; else prisma generate && next build; fi",
  "installCommand": "npm ci",
  "framework": "nextjs"
}
